apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-stas-one.fullname" . }}-test-connection"
  labels:
    {{- include "helm-stas-one.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-stas-one.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
